# scope/

No DSL `scope` stage in 0.8.0 (EXP-SCOPE-0001: NOT JUSTIFIED).

Use:

- `ffvs index --include` / `--exclude` and config (INDEX SCOPE)
- `where path …` / `search … path "…"` for path restriction inside the indexed graph
